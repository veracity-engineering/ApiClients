name: DNV.ApiClients.Veracity.DataPlatform.DataValidatorApi

on:
  workflow_dispatch:
  push:
    branches:
    - "**"
    paths:
    - 'src/DNV.ApiClients.Veracity.DataPlatform.DataValidatorApi/**'
    - '.github/workflows/DNV.ApiClients.Veracity.DataPlatform.DataValidatorApi.yml'
    pull_request:
      types: [ closed ]
      paths:
      - 'src/DNV.ApiClients.Veracity.DataPlatform.DataValidatorApi/**'
      - '.github/workflows/DNV.ApiClients.Veracity.DataPlatform.DataValidatorApi.yml'
jobs:
  nuget-publish:
    uses: ./.github/workflows/nuget-publish.yml
    with:
      source-path: 'src/DNV.ApiClients.Veracity.DataPlatform.DataValidatorApi'
    secrets: inherit