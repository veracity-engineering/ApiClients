name: Veracity.ApiClients.DataPlatform.ProvisionApi

on:
  workflow_dispatch:
  push:
    branches:
    - "**"
    paths:
    - 'src/Veracity.ApiClients.DataPlatform.ProvisionApi/**'
    - '.github/workflows/Veracity.ApiClients.DataPlatform.ProvisionApi.yml'
    pull_request:
      types: [ closed ]
      paths:
      - 'src/Veracity.ApiClients.DataPlatform.ProvisionApi/**'
      - '.github/workflows/Veracity.ApiClients.DataPlatform.ProvisionApi.yml'
jobs:
  nuget-publish:
    uses: ./.github/workflows/nuget-publish.yml
    with:
      source-path: 'src/Veracity.ApiClients.DataPlatform.ProvisionApi'
    secrets: inherit