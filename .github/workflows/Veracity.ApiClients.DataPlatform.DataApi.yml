name: Veracity.ApiClients.DataPlatform.DataApi

on:
  workflow_dispatch:
  push:
    branches:
    - "**"
    paths:
    - 'src/Veracity.ApiClients.DataPlatform.DataApi/**'
    - '.github/workflows/Veracity.ApiClients.DataPlatform.DataApi.yml'
    pull_request:
      types: [ closed ]
      paths:
      - 'src/Veracity.ApiClients.DataPlatform.DataApi/**'
      - '.github/workflows/Veracity.ApiClients.DataPlatform.DataApi.yml'
jobs:
  nuget-publish:
    uses: ./.github/workflows/nuget-publish.yml
    with:
      source-path: 'src/Veracity.ApiClients.DataPlatform.DataApi'
    secrets: inherit